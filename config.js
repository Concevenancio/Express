// This part has been modified to be able to access the database
const config = {
    db: {
        host: "localhost",
        user: "root",
        password: "",
        database: "clientserver",
    },
    listPerPage: 10
}

module.exports = config;